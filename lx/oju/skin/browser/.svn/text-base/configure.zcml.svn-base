<configure
    xmlns="http://namespaces.zope.org/zope"
    xmlns:browser="http://namespaces.zope.org/browser"
    i18n_domain="oju.skin">

  <!-- 'OJUSkin' Zope 3 browser layer
    Register views, viewlets and resources for this layer to make sure
    they only apply to the 'OJUSkin' theme.
    You can also use this layer to override default Plone browser views
    and viewlets only within the 'OJUSkin' theme.
  -->
  <interface
      interface=".interfaces.IThemeSpecific"
      type="zope.publisher.interfaces.browser.IBrowserSkinType"
      name="OJUSkin"
      />
  
  <!-- Viewlets registration -->

  <browser:viewlet
      name="plone.footer"
      manager="plone.app.layout.viewlets.interfaces.IPortalFooter"
      class=".viewlets.SiteActionsViewlet"
      layer=".interfaces.IThemeSpecific"
      permission="zope2.View"
      />


  <!-- Zope 3 browser resources -->

  <!-- Resource directory for images
    See oju/skin/browser/images/README.txt
    for more information about registering images as Zope 3 browser
    resources.
  -->
  <browser:resourceDirectory
      name="oju.skin.images"
      directory="images"
      layer=".interfaces.IThemeSpecific"
      />

  <!-- Resource directory for stylesheets
    See oju/skin/browser/stylesheets/README.txt
    for more information about registering stylesheets as Zope 3 browser
    resources.
  -->
  <browser:resourceDirectory
      name="oju.skin.stylesheets"
      directory="stylesheets"
      layer=".interfaces.IThemeSpecific"
      />

</configure>
